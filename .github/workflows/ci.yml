name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: cmake
      run: cmake .
    - name: make
      run: make
  tests:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: exercise_1
      run: ./exercise1/ut/DayOfYearTests